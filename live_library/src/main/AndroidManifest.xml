<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.live_library">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.BROADCAST_STICKY" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application>


        <activity android:name=".onepx.OnePixelActivity"/>


        <receiver android:name=".receiver.NotificationClickReceiver">
            <intent-filter>
                <action android:name="CLICK_NOTIFICATION"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".service.LocalService"
            android:exported="true"
            android:process=":local"/>
        <service
            android:name=".service.RemoteService"
            android:exported="true"
            android:process=":remote"/>

        <service
            android:name=".service.JobHandlerService"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <provider
            android:name=".pro_sp.SharedPreferenceProvider"
            android:authorities="com.example.live_library"
            android:exported="false"
            android:process=":remote" />

    </application>
</manifest>
